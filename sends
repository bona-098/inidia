console.log("booking", booking);
                    console.log("totalPeople", totalPeople);

Solusi Perbaikan:
bu
: 
"AHL"
code
: 
"Ghm/2025/03/0316"
creator
: 
"Strategic Planning Admin"
description
: 
"oo"
employee
: 
[]
endDate
: 
"2025-03-06T22:00:00+08:00"
family
: 
[]
ghm_room_id
: 
2
guest
: 
['oo']
id
: 
460
location
: 
"Balikpapan"
requestColor
: 
"#6C757D"
requestStatus
: 
"0"
roomName
: 
"Kamar 2 "
sector
: 
"HO"
startDate
: 
"2025-03-06T10:00:00+08:00"
text
: 
"oo"
totalPeople
: 
"1"
Uncaught ReferenceError: totalPeople is not defined
1. **Modifikasi Fungsi validateBooking():
javascript
Copy
function validateBooking() {
  const selectedRoom = form.getEditor("ghm_room_id")?.option("value");
  const currentBooking = bookingData.find(b => b.id === appointmentData.id);
  
  // Ambil totalPeople langsung dari data booking
  const totalGuests = currentBooking?.totalPeople || 0;
  
  const roomCapacity = roomsWithLocations.find(room => room.id === selectedRoom)?.roomOccupancy || 0;
  const remainingCapacity = roomCapacity - totalGuests;

  // Update help text
  form.getEditor("ghm_room_id").option("helpText", 
    `Occupancy: ${roomCapacity} | Booked: ${totalGuests} | Remaining: ${remainingCapacity}`);
  
  return { roomCapacity, totalGuests, remainingCapacity };
}
2. **Update Bagian appointmentTooltipTemplate:
javascript
Copy
appointmentTooltipTemplate: function (model) {
  const booking = model.appointmentData;
  const room = roomsWithLocations.find(room => room.id === booking.ghm_room_id);
  const roomOccupancy = room?.roomOccupancy || 0;
  
  // Gunakan totalPeople langsung dari data booking
  const totalPeople = booking.totalPeople || 0; 
  const remainingCapacity = roomOccupancy - totalPeople;

  // ... bagian kode lainnya tetap sama ...
  const tooltipHtml = `
    <div>
      <b>Purpose:</b> ${booking.text || "No Title"}<br>
      ${formatDate(booking.startDate)} - ${formatDate(booking.endDate)}<br>
      <b>Occupancy:</b> ${roomOccupancy}<br>
      <b>Booked:</b> ${totalPeople}  // 👈 Pakai totalPeople dari backend
      <b>Remaining:</b> ${remainingCapacity}<br>
      <!-- ... -->
    </div>`;
  return tooltipHtml;
}
3. **Modifikasi Fungsi getTotalGuestsPerDay():
javascript
Copy
async function getTotalGuestsPerDay(appointments, roomId, startDate, endDate) {
  let dailyGuestCount = 0;
  
  appointments.forEach(appointment => {
    if (appointment.requestStatus == 4) return;
    
    const isSameRoom = appointment.ghm_room_id === roomId;
    const isInDateRange = (
      new Date(appointment.startDate) <= endDate && 
      new Date(appointment.endDate) >= startDate
    );
    
    if (isSameRoom && isInDateRange) {
      // Gunakan totalPeople langsung dari data
      dailyGuestCount += appointment.totalPeople || 0;
    }
  });
  
  return dailyGuestCount;
}
Penjelasan Perubahan:
Langsung Menggunakan totalPeople - Tidak perlu menghitung manual karena data sudah valid dari backend

Konsistensi Data - Selalu menggunakan sumber data yang sama (backend) untuk semua tampilan

Optimasi Performa - Mengurangi komputasi klien dengan memanfaatkan data yang sudah diproses server

Verifikasi:
Pastikan response API di loadData() mengandung field totalPeople

Cek di console log untuk memastikan data yang diterima sudah benar:

javascript
Copy
console.log("Booking Data:", booking);
Jika implementasi ini masih tidak bekerja, perlu dicek:

Apakah backend benar-benar mengirim field totalPeople

Apakah format data sesuai (harus number)

Apakah ada caching data yang menyebabkan data tidak terupdate
