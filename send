fetch(`http://127.0.0.1:8000/ghm_booking`)
    .then(response => response.json())
    .then(response => {
        console.log("🔍 Data Loaded from API:", response);

        // Ambil hanya data array dari respons
        let data = response.data;

        if (!Array.isArray(data) || data.length === 0) {
            console.warn("⚠️ Warning: Data kosong atau bukan array! Cek API backend.");
            return;
        }

        // Reset dan update Scheduler dengan data terbaru
        schedulerInstance.option("dataSource", null);
        schedulerInstance.repaint();
        setTimeout(() => {
            schedulerInstance.option("dataSource", data);
            schedulerInstance.repaint();
            console.log("✅ Scheduler Updated!");
        }, 500);
    })
    .catch(error => {
        console.error("❌ API Fetch Error:", error);
    });
