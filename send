function loadData() {
    let dataSource = schedulerInstance.getDataSource();

    // Hapus cache data lama
    dataSource.store().clear();
    dataSource.store().push([]);

    // Reload data dari backend
    dataSource.reload().done(function() {
        console.log("Data successfully reloaded");

        // Load ulang data untuk memastikan data terbaru
        dataSource.load().done(function(data) {
            console.log("New Data Loaded:", data); // Debugging: Cek apakah data muncul dari backend

            if (data.length === 0) {
                console.warn("Warning: Data kosong! Cek API backend.");
            }

            schedulerInstance.option("dataSource", data); // Perbarui Scheduler
            schedulerInstance.repaint(); // Re-render Scheduler
        }).fail(function(error) {
            console.error("Failed to load new data:", error);
        });

    }).fail(function(error) {
        console.error("Failed to reload data:", error);
    });
}
